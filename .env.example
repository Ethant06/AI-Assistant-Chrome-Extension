# Used by docker-compose.yml for the Postgres service only.
# Copy to .env in this directory, or set the same names in backend/.env
# and export them before `docker compose up`.

POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=study_assistant
POSTGRES_PORT=5433
